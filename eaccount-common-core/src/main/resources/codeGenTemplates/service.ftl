package ${code.packageName}.service;

import com.ucsmy.core.service.BasicService;
import ${code.packageName}.entity.${code.entityName};

public interface ${code.entityName}Service extends BasicService<${code.entityName}> {
}
